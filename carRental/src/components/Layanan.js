import { StyleSheet } from 'react-native'

const Layanan = StyleSheet.create({
    container: {
        paddingHorizontal: 16,
        paddingTop: 32,
    },
    iconLayanan: {
        flexDirection: 'row',
        justifyContent: "space-between",
    }
})

export default Layanan